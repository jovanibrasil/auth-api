# Define JWT key
jwt.secret=MY_KEY
# Define token expiration time (seconds) - 604800 seconds = 7 days
jwt.expiration=604800

server.port=8083

# Disable the default loggers
#logging.level.org = WARN
#logging.level.blog = WARN

# Datasource: Mysql credentials and url
spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://mysql-database:3306/usersdb}
spring.datasource.username=root
spring.datasource.password=root

# Disable JPA
spring.jpa.hibernate.ddl-auto=none
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

#posts.max-posts=20
# Define profile - It can be defined using annotation (@ActiveProfiles("test"))
# or command line java -Dspring.profiles.active=test -jar app_name.jar 
#spring.profiles.active=test

spring.flyway.locations=classpath:/db/migration,classpath:/db/testdata